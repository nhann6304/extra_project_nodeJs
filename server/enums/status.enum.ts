export enum EStatus {
    INTERVIEW = "interview",
    DECLINED = "declined",
    PENDING = "pending",
    FAILED = "failed",
    PAID = "paid",
    DELIVERED = "delivered",
    CANCELED = "canceled",
}